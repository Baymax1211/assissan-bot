import { activities } from './activities.js';
import { ClientIntents, ClientPartials } from "./configs.js";

// Set the client configs
export const  client = {
    activities,
    ClientIntents,
    ClientPartials
};